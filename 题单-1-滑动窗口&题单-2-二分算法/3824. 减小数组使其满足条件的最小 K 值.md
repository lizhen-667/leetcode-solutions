
nums[i]>0 对于nums中的数都至少需要进行一次-k操作 
k可以取最大的nums中的数
max_element(nums.begin(),nums.end());
k²>=length
如果小于就return false
要使得满足条件的最小次数为 一定为当k大于nums中的每一个数时 此时每个数只用-一次

所以能够确定的满足条件的可能最大值就为k

int R=100000;
int L=1;

while(R>=L){
int mid=L+(R-L)/2;
if(check(mid)){
R=mid-1;
}
else{
L=mid+1;
}
}
return L;


auto check=[&](int k)->bool{
long long k2=k*k;
for(int i=0;i<nums.size();i++){
k2-=(nums[i]-1+k)/k;
if(k2<0){
return false;
}
}
return k2>=0?true:false;

};




