解决这道题目我困惑的最大的点
在于：
如何计算包含相同字符的最大字串的长度

vector<int> svec[26];
int sum=0;
for(int i=0;i<s.size();i++){
sum++;
if(i+1===s.size()||s[i+1]!=s[i]){
svec[s[i]-'a'].push_back(sum);
sum=0;

}
}

int ans=0;
for(auto& a:svec) {
if(a.empty())
continue;
ranges::sort(a,greater());
svec.push_back(0);
svec.push_back(0);
ans=max({ans,a[0]-2,min(a[0]-1,a[1]),a[2]});

}

return ans ? ans: -1;
}