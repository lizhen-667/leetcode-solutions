int R=*max_element(candies.begin(),candies.end());
int L=1;
long long sum=0;
sum=accumulate(candies.begin(),candies.end(),0LL);
if(sum<k){
return 0;
}
auto check=[&](int n)->bool{
long long count=0;
for(int i=0;i<candies.size();i++){
long long t=(long long)candies[i]/n;
count+=t;
}
if(count>=k){
return true;
}
return false;
};

while(R>=L){
int mid=L+(R-L)/2;
if(check(mid)){
L=mid+1;
}
else{
R=mid-1;
}
}
return R;

存在问题：
1.对于二分算法的闭区间写法不够熟练 L=mid+1 R=mid-1 由于这道题目是参数越大 符合条件的概率越低 所以要求符合条件的最大数值 一定是满足条件的区间中最大的数 即左区间的最右边的数

2.对于accumulate(candies.begin(),candies.end(),0LL);
对于第三个参数：它是作为累加的初始值 它的类型为LL类型
一定要看清参数的类型