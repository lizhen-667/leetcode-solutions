# 算法解题通用思维框架：“四步降维法”

> **核心理念**：将算法题从“代码实现”降维到“数学抽象”，先证后写，杜绝边写边猜。

---

## 第一部分：四步核心流程（顺序执行，不可跳跃）

### 第一步：纯数学建模（前3-5分钟，不动键盘）

**动作**：把题目的文字描述转化为**最简数学公式**。  
**目标**：彻底摆脱“循环、指针、数组下标”等代码细节，只关注数学关系。

- **错误做法**：读完题立刻想“是不是用双指针/滑动窗口/DP？”
    
- **正确做法**：拿笔写下题目的核心表达式。例如本题：Ans=max⁡∣∑k=ijnums[k]∣Ans=max∣∑k=ij​nums[k]∣。
    
- **产出物**：一个明确的数学式子。
    

---

### 第二步：公式变形与特征提取（寻找“本质”）

**动作**：盯着公式，进行代数变形，并挖掘其几何/代数意义。  
**目标**：将复杂的题目条件剥离开，找到决定答案的“元变量”。

- **问自己3个问题**：
    
    1. 这个式子能否套用恒等式？（如连续和 →→ 前缀和相减 S[j]−S[i]S[j]−S[i]）
        
    2. 去掉题目限制（如下标顺序），它是什么数学对象？（本题中 →→ 数轴上两点之间的距离）
        
    3. 求这个对象的极值，由谁决定？（本题中 →→ 仅由全局最大值和最小值决定）
        
- **产出物**：一个经过简化的新表达式（如 Ans=max⁡(S)−min⁡(S)Ans=max(S)−min(S)）。
    

---

### 第三步：构造“逻辑证明”（先证后写）

**动作**：用自然语言证明你的解法为什么对，包含充分性和必要性。找反例是验证真理的唯一标准。  
**目标**：确保思路在逻辑上无懈可击，避免写出半对半错的代码。

- **正面（充分性）**：我取到这个极值后，原题一定存在对应的合法结构吗？
    
- **反面（必要性）**：原题的任意合法结构，其结果一定不超过我取的这个极值吗？（即上界证明）
    
- **灵魂拷问**：能不能找到一个反例推翻我的解法？（如果找不到，解法成立）
    
- **产出物**：一个坚定的“解法成立”的信念。
    

---

### 第四步：伪代码与边界锚定（最后才写代码）

**动作**：先写自然语言伪代码，再转换成语法代码。在写之前，先锚定三个特殊边界。  
**目标**：一次性通过编译和极端用例，杜绝补丁式编程。

- **伪代码**：如 `1. 初始化前缀和cur=0, minPre=0, maxPre=0; 2. 遍历数组更新...`
    
- **三大边界必查项**：
    
    1. **空集/零值**：空数组或初始状态是否应参与计算？（如前缀和 `0`）
        
    2. **极值初始化**：找最大值应初始为 `INT_MIN`，找最小值应初始为 `INT_MAX`；还是业务上直接初始化为 `0`？
        
    3. **整数溢出**：做 `INT_MAX - INT_MIN` 是否溢出？是否需要转 `long`？
        

---

## 第二部分：🚫 常见的错误思维习惯（避坑指南）

|错误习惯|导致后果|
|---|---|
|看完题目直接猜“该用什么工具/算法模板”|把解题变成“撞大运”，猜错方向满盘皆输|
|直接在脑子里跑 `for` 循环模拟过程|深陷局部细节，忽略整体数学性质（如顺序无关性）|
|写出代码后才开始找反例验证|心态崩溃，为了修 bug 疯狂打补丁，代码变得臃肿不堪|

---

## 第三部分：✅ 实战自查清单（思考时的自我拷问）

拿起一道新题时，按此清单顺序强制逼问自己：

- □ 
    
    **公式化**：这道题的数学表达式是什么？
    
- □ 
    
    **变形**：能否通过前缀和、差分、对数等工具简化该表达式？
    
- □ 
    
    **抽象意义**：简化后的表达式在几何（距离、面积）或代数（极值、斜率）上代表什么？
    
- □ 
    
    **顺序影响**：原数组的下标顺序，对这个“抽象意义”的求解有影响吗？（本题无影响，很多题有影响）
    
- □ 
    
    **边界覆盖**：全正数、全负数、空集/零值情况下，我的公式依然成立吗？
    
- □ 
    
    **初始化验证**：代码中 `max` 和 `min` 的初始值是否具备物理意义（而非仅仅是随机的大数）？
    

---

**笔记心得**：当你发现一道题的解法很“巧妙”时，往往是你在**第二步（抽象变形）**中看穿了本质，而不是你的代码技巧有多高超。**先练数学眼力，再练码力**，这是通向高手的必经之路