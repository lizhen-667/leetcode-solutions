  if(start==nums.size()||nums[start]!=target)
  这段条件判断代码 我最初的写法时将两个条件的前后顺序进行了颠倒
  如下：
  if(nums[start]!=target||start==nums.size()){
    我先判断
    nums[start]是否等于target
    然后再判断
    start这数组下标变量是否等于nums.size()
    由于代码是按顺序执行的
    当start=nums.size()时 nums[start]此时已经越界 访问不到下标start的值
    所以写if语句条件判断时 一定要注意顺序
    
  }