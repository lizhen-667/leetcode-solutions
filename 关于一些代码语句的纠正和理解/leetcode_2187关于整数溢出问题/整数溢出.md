c++中long long类型的范围是[-2^63,2^63-1] 10进制的精度范围是 10的18次方
int类型的范围是[-2^31,2^31-1] 10进制的精度范围是 10的9次方
所以 当两个较大的int类型的数相乘时，可能会导致溢出 例如：int a=1e7，int b=1e7；
则 a*b=1e14 此时已经超出了int类型的范围 所以要进行显示类型转换
long long c=(long long)a*b;
