例如：
long long r = (long long)maxtime * ((h * (h + 1)) / 2);
如果h取1e7，由于加了括号，运算顺序由原本的从左自右运算变为先计算h*(h+1) 则结果变为1e14，而int类型的数据的最大的数量级为1e9，所以会溢出
如果没有加括号long long r = (long long)maxtime * h * (h + 1)/ 2; 则long类型maxtime会先与h进行计算，计算结果会被转化变为long类型 这时再进行计算都会被转为long类型 结果不会溢出 
所以这个计算顺序十分重要 我们要清楚哪些数字的乘积会溢出，哪些不会溢出