输入：n趟列车的行驶距离数组 int dist[]，通勤总时间 double hour 

dist[i]
输出：最小正整数时速 int speed

由于题目说测试用例的答案不超过10的7次方 也就是说最小速度的范围已经明确了(0,10的7次方] 且hour的小数点后最多存在2位数字

由于速度越大越能满足条件
假设speed满足条件
speed1>speed
则speed1也满足条件

用二分算法解决这道问题
有边界R=10的7次方
如果用10的7次方去检验 发现无法满足条件则这种情况下无解 无法准时达到
auto check=[&](int speed)->{
double count=0;
for(int i=0;i<n-1;i++){
count+=ceil((double)dist[i]/speed)；

}
count+=（double）dist[i]/speed;

if(count>hour){
return false;
}
return true;



}；
int L=0;
int R=10的7次方;
if(!check(R)){
return -1;
}
wihle(R>=L){
int mid=L+(R-L)/2;
if(check(mid)){
R=mid-1;
}
else{
L=mid+1;
}

}
return L;


